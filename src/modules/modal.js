const modalModule = () => {
    const modal = document.querySelector('.header-modal')
    const overlay = document.querySelector('.overlay')
    const button = document.querySelector('.header-modal-button')

    button
}

export default modalModule